bin/
obj/
*.blend1
*.user

.vs

.fake
.ionide
# MSBuild generates these when building the solution; the Rhino repo ignores them
# too, but a submodule does not inherit the parent .gitignore.
*.metaproj
*.metaproj.tmp

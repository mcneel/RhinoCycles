RhinoCycles
===========

The Cycles integration plug-in for Rhinoceros 3D.

For the current state of the Cycles 5.2 port - how it is structured, how to
build it, what is open and what is hacked - see `RHINO-CYCLES-5.md` at the root
of the cycles repository (`../cycles`).
